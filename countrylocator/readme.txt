Copy this jar file into a new directory under your plugins directory.

This plugin uses MaxMind's GeoIP Database (see GeoIP-LICENCE.txt for license)
If you have a newer GeoIP database (currently found at 
http://www.maxmind.com/download/geoip/database/ ) you can place it in the same
plugin folder as this jar file is in, and it will be used.